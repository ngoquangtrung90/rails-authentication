<div class="container">
  <div class="row">
    <div class="col">
      <h1>Pages#home</h1>
      <p>Find me in app/views/pages/home.html.erb</p>

      <% if user_signed_in? %>
        <p>You are logged in.</p>
        <%= link_to 'Discover restaurant', restaurants_path, class: "btn btn-primary" %>
      <% else %>
        <p>You are not logged in.</p>
      <% end %>
    </div>
  </div>
</div>
